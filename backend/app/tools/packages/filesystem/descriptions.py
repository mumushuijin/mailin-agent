from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class ToolDoc:
    summary: str
    description: str


TOOL_DOCS: dict[str, ToolDoc] = {
    "read_file": ToolDoc(
        summary="读取工作区内 UTF-8 文本文件，支持按行分页",
        description="""一句话功能：读取工作区沙箱内指定路径的 UTF-8 文本文件，支持按行分页，超长内容自动分段。

适用场景：
- 用户要求查看、分析或引用某个笔记、配置、代码文件的内容
- 编辑前需要先了解文件现有内容
- 读取 tool_results/ 下落盘的工具结果原文（务必分页，见下）

不适用场景：
- 只需要知道某目录下有哪些文件 → 用 list_directory（热工具，直接调用）
- 需要运行 git / npm / pip 等终端命令 → 用 run_shell（热工具，直接调用）
- 需要在多个文件中查找关键词或正则 → 用 search_files（热工具，直接调用）
- 需要按文件名模式批量找文件 → 用 glob_search（热工具，直接调用）
- 文件是图片、PDF、二进制等非 UTF-8 文件
- 文件超过 512KB → 工具会拒绝，应分段处理

参数说明：
- file_path（必填）：相对工作区根目录的文件路径，如 notes/todo.md、CONFIG.json；不要用绝对路径
- offset（可选，默认 1）：起始行号（1 起始），用于读取下一段
- limit（可选）：读取行数；缺省时读到文件末尾或单次 token 上限为止

关于落盘工具结果（tool_results/...）：
- 这类文件是之前超大工具输出被自动落盘的原文，通常很大
- 单次读取会被强制限制在 token 上限内，不可能也不需要一次读完
- 需要更多内容时，按返回结尾的「[分页]」提示用 offset 继续读取下一段
- 若只想找其中某个信息，优先用 search_files 在该文件内检索，而不是顺序通读

返回格式：
- 成功：每行 `行号|内容`（1 起始）；若有后续内容，结尾附「[分页]」说明总行数与如何继续
- 失败：返回中文错误说明（文件不存在、路径是目录、文件过大、无法解码等）；不要编造文件内容

风险限制：
- 仅可访问工作区（workspace/）内的路径，越界路径会被拒绝
- 只读操作，不修改任何文件
- 单次最多读取 512KB，且受单次 token 上限约束（超出自动分页）""",
    ),
    "write_file": ToolDoc(
        summary="向工作区内写入或覆盖 UTF-8 文本文件",
        description="""一句话功能：将给定内容写入工作区指定路径，若文件已存在则完全覆盖。

适用场景：
- 用户要求创建新文件并写入内容
- 用户要求整体重写某个文件（而非局部修改）
- 需要保存助手生成的笔记、代码、配置等到工作区

不适用场景：
- 只需修改文件中一小段文字 → 用 replace_in_file
- 只需读取文件 → 用 read_file
- 用户未明确要求写入，或内容尚不确定 → 不要主动写入
- 目标路径是目录 → 应使用 mkdir 或指定具体文件路径

参数说明：
- file_path（必填）：相对工作区根目录的目标文件路径，如 `artifacts/weather.html`；HTML/JS/CSS 等裸文件名会自动写入 `artifacts/`
- content（必填）：要写入的完整 UTF-8 文本内容

返回格式：
- 成功：返回「已写入: {file_path}」
- 失败：返回中文错误说明（路径越界等）

风险限制：
- 仅可写入工作区沙箱内；会自动创建缺失的父目录
- 覆盖写入，无法撤销；写入前如不确定应先 read_file 确认
- 不能写入工作区外的系统路径""",
    ),
    "replace_in_file": ToolDoc(
        summary="替换工作区文本文件中的指定片段",
        description="""一句话功能：在已有文件中查找 old_text 并替换为 new_text，默认只替换第一处。

适用场景：
- 用户要求修改文件中某段文字、变量名、配置项等
- 已知要替换的精确原文（或足够唯一的片段）
- 局部编辑，不希望重写整个文件

不适用场景：
- 需要创建新文件或整文件重写 → 用 write_file
- 不确定 old_text 的精确内容 → 应先 read_file 确认再替换
- 需要批量重命名文件路径 → 用 move_file
- 需要在多个文件中搜索替换 → 应逐个文件处理或先用 search_files 定位

参数说明：
- file_path（必填）：相对工作区根目录的文件路径
- old_text（必填）：要被替换的原文，必须与文件中内容完全匹配（含空格、换行）
- new_text（必填）：替换后的新文本
- replace_all（可选，默认 false）：true 时替换文件中所有匹配项；false 时仅替换第一处

返回格式：
- 成功：返回「已编辑: {file_path}（替换 N 处）」
- 失败：返回中文错误说明（文件不存在、未找到 old_text 等）；未找到时不修改文件

风险限制：
- 仅可修改工作区内的 UTF-8 文本文件
- old_text 不唯一且 replace_all=false 时只改第一处，可能造成遗漏
- 修改不可自动撤销；重要文件建议先 read_file 备份关键内容""",
    ),
    "list_directory": ToolDoc(
        summary="列出工作区某目录下的文件和子目录",
        description="""一句话功能：列出指定目录下一层的所有文件和子目录名称及类型。

适用场景：
- 用户询问「有哪些文件」「目录结构是什么」
- 在读写文件前需要确认路径是否存在、名称是否正确
- 探索工作区顶层或某个子目录的内容

不适用场景：
- 需要递归列出所有深层文件 → 用 glob_search（如 **/*.py）
- 需要在文件内容中搜索关键词 → 用 search_files
- 需要读取某个文件内容 → 用 read_file
- 需要按模式匹配文件名 → 用 glob_search

参数说明：
- directory_path（可选，默认 "."）：相对工作区根目录的目录路径；"." 表示工作区根目录

返回格式：
- 成功：每行一条，格式为 [file] 相对路径 或 [dir] 相对路径；空目录返回「（空目录）」
- 超过 200 条时截断并提示
- 失败：返回中文错误说明（目录不存在、路径不是目录等）

风险限制：
- 仅列出工作区沙箱内的目录
- 只列当前目录下一层，不递归
- 单次最多返回 200 条""",
    ),
    "search_files": ToolDoc(
        summary="在工作区文件内容中按正则搜索（忽略大小写）",
        description="""一句话功能：在指定目录下递归搜索 UTF-8 文本文件，用正则匹配内容并返回路径与行号（热工具，可直接调用）。

适用场景：
- 用户问「哪个文件里写了 XXX」「帮我找包含某关键词或模式的笔记/代码」
- 不确定目标文件路径，但知道要找的文字或正则
- 需要在 .md、.py 等文本文件中定位信息

不适用场景：
- 已知确切文件路径，只需读取全文 → 用 read_file
- 只需按文件名模式查找（如所有 .md 文件）→ 用 glob_search
- 搜索二进制文件、图片、PDF 内容
- 需要搜索工作区外的文件

参数说明：
- query（必填）：Python 正则；非法正则会立刻报错且不扫盘。普通关键词也可直接用
- directory_path（可选，默认 "."）：搜索起点的相对目录
- file_pattern（可选，默认 "*"）：文件名过滤，如 *.md、*.py
- max_results（可选，默认 20，上限 30）：最多返回多少个匹配文件

返回格式：
- 成功：每个匹配文件一块，含相对路径和最多 3 行匹配预览（L行号: 内容）
- 末尾附匹配文件总数；无结果时明确说明
- 失败：返回中文错误说明（含无效正则）

风险限制：
- 仅搜索工作区沙箱内、可 UTF-8 解码的文本文件
- 跳过二进制文件；不搜索文件外的内容
- 单次最多 30 个匹配文件""",
    ),
    "glob_search": ToolDoc(
        summary="按 glob 模式匹配工作区内的文件或目录路径",
        description="""一句话功能：从指定目录出发，按 glob 模式匹配文件和目录的相对路径。

适用场景：
- 用户问「有哪些 .md 文件」「src 下所有 Python 文件」
- 需要批量定位符合命名规则的文件路径
- 在批量操作前先获取目标文件列表

不适用场景：
- 需要在文件内容中搜索文字 → 用 search_files
- 只需看某目录下一层有什么 → 用 list_directory
- 已知唯一文件路径，直接 read_file 即可
- 需要搜索工作区外的路径

参数说明：
- glob_pattern（必填）：glob 模式，如 *.md、**/*.py、src/**/test_*.py
- directory_path（可选，默认 "."）：匹配起点的相对目录

返回格式：
- 成功：每行 [file] 或 [dir] 加相对路径；无匹配时明确说明
- 超过 200 条时截断并提示
- 失败：返回中文错误说明

风险限制：
- 仅匹配工作区沙箱内的路径
- 单次最多返回 200 条
- 模式为空会被拒绝""",
    ),
    "mkdir": ToolDoc(
        summary="在工作区内创建目录",
        description="""一句话功能：在工作区指定路径创建目录，可选自动创建上级目录。

适用场景：
- 写入文件前需要确保目录存在（write_file 也会自动创建，但显式 mkdir 更清晰）
- 用户要求新建文件夹、整理目录结构
- 批量操作前准备目标目录

不适用场景：
- 目的是创建并写入文件 → 直接用 write_file（会自动创建父目录）
- 需要移动已有文件到目录 → 用 move_file
- 需要删除目录 → 空目录可用 delete_file，非空目录需先清空

参数说明：
- directory_path（必填）：相对工作区根目录的目录路径，如 notes/2026
- parents（可选，默认 true）：true 时连同缺失的上级目录一并创建

返回格式：
- 成功：返回「已创建目录: {directory_path}」
- 目录已存在时不报错（exist_ok）
- 失败：返回中文错误说明（路径越界等）

风险限制：
- 仅可在工作区沙箱内创建目录
- 不能创建文件，只创建目录""",
    ),
    "move_file": ToolDoc(
        summary="移动或重命名工作区内的文件/目录",
        description="""一句话功能：将 source_path 移动或重命名到 destination_path。

适用场景：
- 用户要求重命名文件或目录
- 用户要求将文件移动到另一个目录
- 整理工作区文件结构

不适用场景：
- 只需修改文件内容 → 用 replace_in_file 或 write_file
- 只需复制内容到新文件（保留原文件）→ 应 read_file 后 write_file 到新路径
- 源路径不存在 → 应先 list_directory 或 glob_search 确认
- 需要移动工作区外的文件

参数说明：
- source_path（必填）：源文件或目录的相对路径
- destination_path（必填）：目标相对路径（含新文件名或新目录位置）

返回格式：
- 成功：返回「已移动: {source} -> {destination}」
- 失败：返回中文错误说明（源不存在、路径越界等）

风险限制：
- 源和目标均须在工作区沙箱内
- 移动操作不可自动撤销；目标已存在时可能被覆盖（系统行为）
- 会自动创建目标路径的父目录""",
    ),
    "delete_file": ToolDoc(
        summary="删除工作区内的文件或空目录",
        description="""一句话功能：删除工作区指定路径的文件；若为空目录也可删除。

适用场景：
- 用户明确要求删除某个文件
- 用户要求清理不再需要的草稿、临时文件
- 删除已清空的目录

不适用场景：
- 用户未明确要求删除 → 禁止主动调用
- 需要删除非空目录 → 工具会拒绝，需先逐个删除内容
- 只需修改文件内容 → 用 replace_in_file
- 不确定路径或文件是否仍需保留 → 应先与用户确认

参数说明：
- file_path（必填）：相对工作区根目录的文件路径；空目录也可传入目录路径

返回格式：
- 成功：返回「已删除: {file_path}」或「已删除空目录: {file_path}」
- 失败：返回中文错误说明（不存在、目录非空等）

风险限制：
- 仅可删除工作区沙箱内的路径
- 不可恢复；非空目录无法删除
- 高风险操作：必须在用户明确授权后使用，勿批量删除""",
    ),
}
